<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="CustomWindowTitleBackground">
       <item name="android:background">#337ffd</item>
	</style>
	<style name="CustomWindowBackground">
       <item name="android:background">#e5e3e3</item>
	</style>
	
    <!-- Application theme. -->
    <style name="SealAppTheme" parent="android:Theme">
     <item name="android:windowTitleSize">@dimen/base_dimen_90</item>
     <item name="android:windowTitleBackgroundStyle">@style/CustomWindowTitleBackground</item>
     <item name="android:windowAnimationStyle">@style/activityAnim</item>
	</style>
	
    <style name="activityAnim">
        <item name="android:activityOpenEnterAnimation">@anim/popshow_anim</item>
        <item name="android:activityOpenExitAnimation">@anim/pophidden_anim</item>
    	<item name="android:activityCloseEnterAnimation">@anim/popshow_anim</item>
    	<item name="android:activityCloseExitAnimation">@anim/pophidden_anim</item>

    </style>
	
    <style name="loading_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowFrame">@null</item> <!-- 边框 -->
        <item name="android:windowIsFloating">true</item> <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item> <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item> <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item> <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item> <!-- 模糊 -->
        <item name="android:clickable">false</item>
        
        <item name="android:backgroundDimAmount">0.7</item>
    </style>
    
    <style name="PopupWindowAnimation">  
        <item name="android:windowEnterAnimation">@anim/popshow_anim</item>  
        <item name="android:windowExitAnimation">@anim/pophidden_anim</item>  
    </style>
    
    <style name="Dialog_Message">
	    <!--item name="android:textColor">@color/dialog_message_text</item-->
	    <item name="android:textColor">@android:color/white</item>
	    <item name="android:gravity">center</item>
	</style>
	
    <style name="AlertDialog" parent="@android:Theme.DeviceDefault.Light.Dialog">
	<!-- 这里设置背景为透明，为了隐藏边框 -->
	        <item name="android:windowBackground">@android:color/transparent</item>
	        <item name="android:windowNoTitle">true</item>
	<!-- 这里是修改顶部标题背景颜色，具体颜色自己定，可以是图片 -->
	        <item name="android:topDark">@color/white</item>
	 <!-- 这里是修改内容区域背景颜色 -->
		<item name="android:centerDark">@color/white</item>
	        <item name="android:windowIsTranslucent">true</item>
	        <item name="android:windowAnimationStyle">@+android:style/Animation.Translucent</item>
	</style>

</resources>
